/*
Styled Components is a CSS in JS library Solutions for React and React-native
It uses tagged template literals that allows you to write plain CSS that is scoped to a single component inside your javasript code.
The advantage of using styled component is that it is adopted by large number of companies and its one of the most starred libraries in the react ecosystem

Features/ Benefits
-Automatic critical CSS
-No claasName bugs
-Easier deletion of CSS
-Dynamic styling
-Painless maintenance
-Automatic vendor prefixing

React specific linting rules for eslint
Installation
npm install eslint eslint-plugin-react --save-dev

Assuming you already have ESLint installed, run:

# npm
npm install eslint-plugin-react-hooks --save-dev

PseudoClasses e.g Styling elements when you hover over them.




*/